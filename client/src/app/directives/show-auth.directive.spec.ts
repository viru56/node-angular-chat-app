import { ShowAuthDirective } from './show-auth.directive';

describe('ShowAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAuthDirective();
    expect(directive).toBeTruthy();
  });
});
